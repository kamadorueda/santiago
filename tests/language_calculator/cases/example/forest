---
sum := rules "sum" "subtract" "sum"
  sum := rules "sum" "plus" "sum"
    sum := lexemes "INT"
      INT "1" (1, 1)
    plus := lexemes "+"
      + "+" (1, 3)
    sum := rules "sum" "divide" "sum"
      sum := rules "sum" "multiply" "sum"
        sum := lexemes "INT"
          INT "2" (1, 5)
        multiply := lexemes "*"
          * "*" (1, 7)
        sum := lexemes "INT"
          INT "3" (1, 9)
      divide := lexemes "/"
        / "/" (1, 11)
      sum := lexemes "INT"
        INT "4" (1, 13)
  subtract := lexemes "-"
    - "-" (1, 15)
  sum := lexemes "INT"
    INT "5" (1, 17)