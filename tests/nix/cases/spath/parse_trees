---
expr_app := rules "expr_app" "expr_select"
  expr_app := rules "expr_app" "expr_select"
    ID := lexemes "ID"
      ID "import" (1, 1)
    SPATH := lexemes "SPATH"
      SPATH "<nixpkgs>" (1, 8)
  expr_simple := rules "{" "binds" "}"
    { := lexemes "{"
      { "{" (1, 18)
    binds := rules 
    } := lexemes "}"
      } "}" (1, 19)